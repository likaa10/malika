// File ini dinonaktifkan karena sudah diganti ke loading_rekap.dart
import 'package:flutter/material.dart';

class LoadingTransisiPage extends StatelessWidget {
  const LoadingTransisiPage({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Didegradasi')));
  }
}